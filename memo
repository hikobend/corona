CREATE TABLE `infection` (
  `id` int AUTO_INCREMENT PRIMARY KEY, 
  `date` date, 
  `name_jp` text, 
  `npatients` int
);

create table `event` (
  `id` int AUTO_INCREMENT PRIMARY KEY,
  `title` text,
  `description` text,
  `begin` date,
  `end` date
);